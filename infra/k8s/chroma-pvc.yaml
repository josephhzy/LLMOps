apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: llm-ops-chroma-pvc
  namespace: llm-ops
  labels:
    app: llm-ops-api
    component: chromadb
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 10Gi
