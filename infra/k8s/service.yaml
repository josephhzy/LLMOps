apiVersion: v1
kind: Service
metadata:
  name: llm-ops-api
  namespace: llm-ops
  labels:
    app: llm-ops-api
spec:
  selector:
    app: llm-ops-api
  ports:
    - port: 80
      targetPort: 8000
      protocol: TCP
  type: ClusterIP
